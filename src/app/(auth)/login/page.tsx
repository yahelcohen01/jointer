// Phase 1 / Slice 1 — placeholder. Slice 3 (issue #5) wires up Supabase Auth
// with magic link + Google OAuth.
export default function LoginPage() {
  return (
    <main className="mx-auto max-w-md flex-1 px-4 py-24 flex flex-col items-center text-center gap-4">
      <h1 className="text-3xl font-bold font-display">התחברות</h1>
      <p className="text-muted-foreground">הזדהות עם קישור קסם או Google תוטמע בפרוסה הבאה.</p>
    </main>
  );
}
